# miniProjects
This is a repository to contain mini projects i may do. For example a BigInt implementation... a sudoku solver/generator, and things like that. May any project get out of scope, it will probably be migrated to its own repo.
